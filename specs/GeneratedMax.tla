------------------------------ MODULE GeneratedMax ------------------------------
EXTENDS Naturals, Sequences
CONSTANTS in_arg_0, in_x, in_a

(*--algorithm Math
  variables result_compare_v2

  procedure compare_v2(arg_0, x, a)
  begin
    compare_v2:
      if a > b
      /\ x["operator"] = "<"
      /\ arg_2[1] = 7 then
        result_compare_v2 := a;
        return;
  end procedure

  begin
    compare_v2:
      if result_compare_v2 = defaultInitValue then
        call compare_v2(in_arg_0, in_x, in_a)
      end if;
end algorithm; *)
=========================================================================